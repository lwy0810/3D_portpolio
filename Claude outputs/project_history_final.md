# "프로젝트 이력" 최종 입력값

(참고: 시작월은 Notion 이슈 트래커의 가장 이른 발견일(2026-08-23)을 근거로 2026.8로 추정해 넣었어 — 실제 개발 시작월과 다르면 그 부분만 바꿔서 넣으면 됨)

- **프로젝트 유형**: 드롭다운에 "개인 프로젝트"류 항목 있으면 그걸로, 없으면 "기타" 유지
- **프로젝트 이름**: `3D_portpolio (Unity 턴제 커맨드 배틀 RPG)`
- **소속 회사**: 비워두거나 "개인 프로젝트"
- **시작**: 2026. 8  /  **종료**: 진행중 (또는 폼에 재직중/진행중 체크박스가 있으면 체크)
- **경력 기술 및 수행 직무와 역할** (2500자 이내, 아래 문구는 약 1,345자):

```
[프로젝트명] 3D_portpolio — Unity 기반 턴제 커맨드 배틀 RPG (개인 프로젝트)
[기술 스택] Unity 6000.x, C#, URP, Notion(이슈 관리), 엑셀(테스트케이스)
[역할] 기획·설계·구현·QA 전 과정을 단독으로 수행

가장 좋아하는 콘솔 게임 '영웅전설 섬의궤적4'의 AT(행동력) 기반 전투 시스템을 최대한 가깝게 재현하는 것을 목표로 기획했습니다. 설계 단계에서 원작 공식 위키와 커뮤니티 자료를 직접 분석해 데미지 계산식, 방어력 반영 로직, 속성 상성, AT(행동 순서) 재계산 규칙을 파악하고, 개인 프로젝트 규모에 맞게 단순화해 설계에 반영했습니다.

기본 골격(필드 이동, 몬스터 조우, 커맨드 전투 UI)을 완성한 뒤, 제출 전 스스로 완성도를 검증하기 위해 202개 항목의 테스트케이스 문서를 직접 작성했습니다. Ok/Fail/N-A/Not Test로 상태를 기록하며 하나씩 확인한 결과, 필드 이동 같은 기반 시스템은 대부분 정상이었지만 전투의 핵심인 데미지 계산·명중회피·크리티컬·속성 상성·사망 처리·턴 순서 재정렬에서 33건의 결함을 발견했습니다.

또한 개발 중 발견한 모든 문제를 Notion 이슈 트래커에 유형(버그/기능/리팩터링/에셋/환경/문서)·우선순위(P0~P2)·상태(Backlog~완료)로 분류해 35건을 관리했습니다. 게임 실행 자체를 막는 P0(컴파일 실패, 씬 전환 시 스탯 초기화, 몬스터 조우 트리거 미동작 등)부터 우선 해결하고, 텍스처 품질 같은 P2는 의도적으로 후순위로 미루는 방식으로 진행했습니다.

가장 어려웠던 부분은 전투를 총괄하는 클래스 하나가 카메라 제어, 입력 상태머신, 애니메이션, 턴 진행, UI를 전부 떠안고 있어 하나를 고치면 다른 곳이 깨지는 구조였다는 점입니다. AI 코드 어시스턴트와 함께 코드 구조를 먼저 분석하고, TC 문서와 이슈 트래커의 우선순위를 기준으로 명중/회피 → 방어력 반영 데미지 → 속성 상성 → 크리티컬 판정 로직, 사망/승패 처리, 몬스터 자동 턴, 매 턴 AT(Speed) 재정렬, 후퇴 커맨드를 단계적으로 구현했습니다. 수정한 코드는 실제 Unity 빌드 환경 없이도 문법·타입 오류를 걸러낼 수 있도록 별도 컴파일 검증 과정을 거쳐 확인했습니다.

그 결과 33건의 결함 중 상당수가 실제로 동작하는 전투 루프로 해소됐고, 필드 이동부터 전투까지 실제로 이어서 플레이할 수 있는 프로토타입을 완성했습니다. 이 프로젝트를 통해 상용 게임의 시스템을 스스로 분석해 설계에 반영하는 능력, 문제를 구조화하고 우선순위를 정해 체계적으로 해결하는 능력, 그리고 AI를 코드 생성기가 아니라 구조 분석과 의사결정을 함께하는 협업 파트너로 활용하는 능력을 길렀습니다.
```
